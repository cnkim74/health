# macOS
.DS_Store

# Capacitor 웹 자산 — 빌드 시 sync-www.sh 가 루트 index.html 등에서 자동 생성.
# (git 으로 추적하면 매 빌드마다 수정돼 git pull 이 막히므로 무시)
app/www/

# sync-www.sh 가 매 빌드 재생성하는 앱아이콘(추적 시 git pull 충돌 유발)
app/assets/AppIcon-512@2x.png
app/ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png
