// window.Capacitor.Plugins.Oauth.authenticate({ url, callbackScheme }) → { url: "carenote://login-callback?code=..." }
// ASWebAuthenticationSession 기반: 인앱(모달 시트) 로그인 + 커스텀 스킴 콜백 자동 수신.
module.exports = {};
